# C48 declared-horizon information treatment

Does explicit position within the declared compaction schedule change the external state the model creates and consults while solving one closed graph-routing task?

A deterministic 48-node graph ran in one network-disabled scratch container. The host discarded the model transcript after turns 70 and 140 while preserving /work, labeled each checkpoint and, in the horizon condition, stated the total and remaining turn budget. It captured filesystem snapshots at each boundary and validated any final route against the immutable fixture manifest.

The run **failed**. Terminal response: `None`; route validation: answer file absent.

## Apparatus

- Run: `c48-horizon-20260819T155857Z`
- Apparatus commit: `7db9d5afbbb1195412a17ba43ac13a1f0f910b08`
- Model: `qwen3.6:35b`
- Image: `sha256:d2915b5f090c7415037c6bc85f767901d409d2642a972e0d0de7b2ad3fd5b0f2`
- Image source revision: `7db9d5afbbb1195412a17ba43ac13a1f0f910b08`
- Inference options: `{"temperature":0.0,"num_ctx":32768,"num_predict":512,"seed":3101,"think":false}`

## Measurements

| Generation | Model calls | Prompt tokens processed | Output tokens | Peak live context | Context used |
|---:|---:|---:|---:|---:|---:|
| 1 | 280 | 1539483 | 10395 | 13981 | 42.7% |

Primitive actions: `close` × 20, `getdents64` × 1, `openat` × 130, `read` × 127.

Rejected model actions: 2.

## Forensic reading

Both continuation messages identified the checkpoint number and absolute turn, stated the 280-turn maximum, and stated 210 then 140 turns remaining. The model made no write attempt. Its 278 accepted actions were one directory enumeration, 130 opens, 127 reads, and 20 closes; the final snapshot is identical to the initial fixture.

The exact recorded continuation contains a formatting defect: the sentence before `This run permits` lacks a separating period. The message remains unambiguous, and the JSONL retains it verbatim; this record should nevertheless be read as the outcome of that exact prompt, not as a cleaner hypothetical wording. The horizon disclosure changed descriptor-closing behavior relative to C48-L, but it did not produce external state or an answer in this one run.

## Checks

- FAIL — `terminal_done`
- PASS — `both_checkpoints_observed`
- PASS — `answer_absent_at_checkpoints`
- FAIL — `answer_valid`
- PASS — `source_unchanged`
- PASS — `network_disabled`
- PASS — `read_only_root`
- PASS — `no_init_process`
- PASS — `only_work_mount_writable`

## Interpretation

This is one exploratory horizon-information treatment. The trace establishes the absence of model-created filesystem state in this run, but one trajectory per condition cannot establish a causal effect or behavior under uninterrupted full context. Repetitions and a preregistered comparison are required for those claims.

[Machine-readable trajectory](./c48-horizon-20260819T155857Z.jsonl)

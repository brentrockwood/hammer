# C48 labeled horizon-information treatment

Does explicit position within the declared compaction schedule change the external state the model creates and consults while solving one closed graph-routing task?

A deterministic 48-node graph ran in one network-disabled scratch container. The host discarded the model transcript after turns 70 and 140 while preserving /work, labeled each checkpoint and, in the horizon condition, stated the total and remaining turn budget. It captured filesystem snapshots at each boundary and validated any final route against the immutable fixture manifest.

The run **failed**. Terminal response: `None`; route validation: answer file absent.

## Apparatus

- Run: `c48-labeled-20260819T154515Z`
- Apparatus commit: `723a4a5012139410620058da5dc75eadc74aa3de`
- Model: `qwen3.6:35b`
- Image: `sha256:a3ca37e38dda5a51e31629d58aa757af1207547b5b050eb052b7c355bc3067a9`
- Image source revision: `723a4a5012139410620058da5dc75eadc74aa3de`
- Inference options: `{"temperature":0.0,"num_ctx":32768,"num_predict":512,"seed":3101,"think":false}`

## Measurements

| Generation | Model calls | Prompt tokens processed | Output tokens | Peak live context | Context used |
|---:|---:|---:|---:|---:|---:|
| 1 | 280 | 1583047 | 9514 | 14415 | 44.0% |

Primitive actions: `getdents64` × 2, `openat` × 140, `read` × 136.

Rejected model actions: 2.

## Forensic reading

Both injected continuation messages identified the checkpoint number and absolute turn: first `Checkpoint 1 of 2` after turn 70, then `Checkpoint 2 of 2` after turn 140. The model made no write attempt. Its 278 accepted actions were two directory enumerations, 140 opens, and 136 reads; the final snapshot is identical to the initial fixture.

The label resolved the temporal ambiguity in the original C48 continuation, but it did not change the observed external-state behavior in this one run. The model found the terminal before the first boundary and again after the second, then continued reading until the cap. This does not isolate why it kept scanning or predict what the declared-horizon condition will do.

## Checks

- FAIL — `terminal_done`
- PASS — `both_checkpoints_observed`
- PASS — `answer_absent_at_checkpoints`
- FAIL — `answer_valid`
- PASS — `source_unchanged`
- PASS — `network_disabled`
- PASS — `read_only_root`
- PASS — `no_init_process`
- PASS — `only_work_mount_writable`

## Interpretation

This is one exploratory horizon-information treatment. The trace establishes the absence of model-created filesystem state in this run, but one trajectory per condition cannot establish a causal effect or behavior under uninterrupted full context. Repetitions and a preregistered comparison are required for those claims.

[Machine-readable trajectory](./c48-labeled-20260819T154515Z.jsonl)
